using UnityEngine;

public class FriendProfilePage : MonoBehaviour
{
    //We set these to point to the correct components in the Unity inspector so we can refer to them more easily in the code
    public GameObject nameText;
    public GameObject statusButtonIcon;
    public GameObject statusButtonText;
}
